<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /lyton/public/

    RewriteRule ^uploads/ - [L]

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L,QSA]
</IfModule>
